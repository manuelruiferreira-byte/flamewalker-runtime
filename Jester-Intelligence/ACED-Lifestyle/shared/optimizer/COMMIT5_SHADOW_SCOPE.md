# Commit 5 shadow scope

The optimizer runs beside ACE Mind in read-only mode. It reads the rendered agent-state mirror and supplement tick history, writes comparison records only to its own IndexedDB database, and has no authority over visible recommendations or ACE Mind state.

Acceptance requires the 160 existing registry, layer, optimizer, and sequential audit checks plus the 23 shadow-adapter checks and exactly one module loader in the live HTML.
